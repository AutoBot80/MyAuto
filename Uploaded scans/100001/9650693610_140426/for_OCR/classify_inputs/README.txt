PNG files here are the per-page raster cuts (PDF → RGB, lossless save) used with pre-OCR text for page classification.
Aadhaar photo **front** is detected only when OCR contains both a DOB cue and Male/Female (English/Hindi; any common casing).
Sibling folder ../raw/ holds PDF copies only (no rasters).
