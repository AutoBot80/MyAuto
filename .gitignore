# Environment and secrets
.env
.env.*
!.env.example
!client/.env.production
backend/.env
# AWS keys (never commit)
AWS*.csv
*accessKeys*.csv

# Python
venv/
.venv/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Node
node_modules/
client/node_modules/
client/dist/
electron/node_modules/
electron/release/
electron/release-*/
electron/resources/update-token.json


# IDE and OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs and temp
*.log
logs/
tmp/
temp/
.cache/

# Testing and coverage
.coverage
htmlcov/
.pytest_cache/
.tox/

# Local / override (keep example only)
*.local

# Scanner / local assets (not versioned)
Scanner/

# Hero MISP: persisted KYC insurer strategy (per portal host)
backend/app/data/kyc_insurer_strategy_cache.json

# Playwright browser profile (persists sessions across restarts)
.browser-profile/

# Local login payloads / secrets (never commit)
client/login.json

# Runtime data folders (local scans, OCR output, bulk uploads)
Uploaded scans/
uploaded-scans/
ocr_output/
Bulk Upload/
bulk-upload/

# Terraform: commit .tf and .terraform.lock.hcl; not provider cache or state
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
